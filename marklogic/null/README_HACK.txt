
Added by P. Vanderveen

This is a hack to get rid of an annoying error message running on Windows
Error is that Hadoop binaries can't be found at /null/bin
Well, now they are there.  

NOTE: The real fix would be to define the proper path to these (they come in /mlcp/bin)